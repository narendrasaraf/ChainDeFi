require('dotenv').config();
const { ethers } = require('ethers');
const fs = require('fs');

async function testFull() {
    console.log("Run frontend manually and test loan logic, or trust the fix because `walletAddress` is now populated in DB and checksum is `getAddress()` safe");
}

testFull();
